{
  "name": "bus-fleet-assets",
  "private": true,
  "description": "Front-end assets: Tailwind build and vendored htmx. No runtime npm dependency.",
  "scripts": {
    "build:css": "tailwindcss -i web/src/app.css -o web/static/css/app.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  },
  "dependencies": {
    "htmx.org": "2.0.10"
  }
}
